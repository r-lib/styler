# Assert the transformers

Actually only assert name and version of style guide in order to make
sure caching works correctly.

## Usage

``` r
assert_transformers(transformers)
```

## Arguments

- transformers:

  A list of transformer functions that operate on flat parse tables.
