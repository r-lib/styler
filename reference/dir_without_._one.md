# `dir()`, but with full names, ignored case, and included hidden files and recursive.

[`dir()`](https://rdrr.io/r/base/list.files.html), but with full names,
ignored case, and included hidden files and recursive.

## Usage

``` r
dir_without_._one(path, recursive, ...)
```
