# Serialize all lines for a given column

Serialize all lines for a given column

## Usage

``` r
alignment_serialize_column(relevant_pd_by_line, column)
```

## Arguments

- relevant_pd_by_line:

  A list with parse tables of a multi-line call, excluding first and
  last column.

- column:

  The index of the column to serialize.
