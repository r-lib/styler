# Find `dontrun` and friend sequences

Returns the indices of the lines that correspond to a `dontrun` or
friends sequence.

## Usage

``` r
find_dont_seqs(bare)
```

## Arguments

- bare:

  Bare code.
