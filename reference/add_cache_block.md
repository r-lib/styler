# Add the block id to a parse table

Must be after
[`nest_parse_data()`](https://styler.r-lib.org/reference/nest_parse_data.md)
because requires a nested parse table as input.

## Usage

``` r
add_cache_block(pd_nested)
```

## Arguments

- pd_nested:

  A top-level nest.
