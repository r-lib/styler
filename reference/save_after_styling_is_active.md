# Heuristic to see if a file styled with the addin should be saved or not.

Using the R option `"styler.save_after_styling"` and if unset, checks
legacy method via environment variable `save_after_styling`.

## Usage

``` r
save_after_styling_is_active()
```
