# Set the multi-line column

Sets the column `multi_line` in `pd` by checking row-wise whether any
child of a token is a multi-line token.

## Usage

``` r
set_multi_line(pd)
```

## Arguments

- pd:

  A parse table.
