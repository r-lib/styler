# Create more specs

Syntactic sugar for creating more specs. This is useful when we want to
add more arguments (because we can search for this function in the
source code).

## Usage

``` r
cache_more_specs(include_roxygen_examples, base_indention)
```
