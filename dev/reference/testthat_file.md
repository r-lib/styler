# Create the path to a test that file

Create the path to a test that file

## Usage

``` r
testthat_file(...)
```

## Arguments

- ...:

  Arguments passed to
  [`file.path()`](https://rdrr.io/r/base/file.path.html) to construct
  the path after ".../tests/testthat/"
