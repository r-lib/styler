#!/usr/bin/env Rscript
files <- commandArgs(trailing = TRUE)


if (packageVersion("styler") < "1.1.1.9001") {
# work around for #521.
  lapply(files, styler::style_file)
} else {
  styler::style_file(files)
}
